import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.examenfinal',
  appName: 'examenfinal',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
